<div class="landing">
  <h2>Runaway</h2>
  {{#each model as |projectUnit|}}
    {{#if projectUnit.featured}}
      {{!-- {{project-item project=projectUnit}} --}}
      {{three-player}}
    {{/if}}
  {{/each}}
</div>

{{outlet}}
